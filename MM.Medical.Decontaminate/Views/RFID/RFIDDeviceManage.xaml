<UserControl x:Class="MM.Medical.Decontaminate.Views.RFID.RFIDDeviceManage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MM.Medical.Decontaminate.Views.RFID"
             xmlns:controls="clr-namespace:Ms.Controls;assembly=Ms.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Border Padding="10,0,10,0">
            <Grid>
                <controls:MediaButton FISize="{StaticResource LargeExFontSize}" Click="Add_Click" Content="添加"  HorizontalAlignment="Right"/>
            </Grid>
        </Border>
        <Border Grid.Row="1">
            <ListView>
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="设备编号"></GridViewColumn>
                        <GridViewColumn Header="设备编号"></GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>
        </Border>
    </Grid>
</UserControl>
