<UserControl x:Class="MM.Medical.Client.Views.Video"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MM.Medical.Client"
        d:DesignHeight="768" d:DesignWidth="1080"
        mc:Ignorable="d">
    <Grid Background="{Binding Background, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}}">
        <Image x:Name="img" Source="{Binding ImageSource}" Stretch="Uniform" Margin="5"/>
        <Grid x:Name="canvas" Visibility="Collapsed" Background="#00000000" Width="{Binding ActualWidth,ElementName=img}" Height="{Binding ActualHeight,ElementName=img}" 
            MouseLeftButtonDown="DrawStart_Click" MouseMove="Drawing_Click" MouseLeftButtonUp="DrawComplete_Click">
            <Path x:Name="path" Stroke="Red" StrokeThickness="1" />
        </Grid>
    </Grid>
</UserControl>
