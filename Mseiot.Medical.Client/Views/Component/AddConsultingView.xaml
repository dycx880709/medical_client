<UserControl x:Class="MM.Medical.Client.Views.Component.AddConsultingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:validation="clr-namespace:MM.Medical.Share.Validations;assembly=MM.Medical.Share"
             xmlns:local="clr-namespace:MM.Medical.Client.Views.Component"
             mc:Ignorable="d">
    
    <StackPanel Margin="20">
        <StackPanel Orientation="Horizontal" Height="{StaticResource InputElementHeight}" Margin="0,10" Grid.Row="1">
            <TextBlock Text="诊室名称: " VerticalAlignment="Center"/>
            <TextBox>
                <TextBox.Text>
                    <Binding Path="Name" UpdateSourceTrigger="PropertyChanged" Mode="TwoWay" NotifyOnValidationError="True">
                        <Binding.ValidationRules>
                            <validation:StringLengthValidationRule Title="诊室名称" ValidatesOnTargetUpdated="True" MinValue="1" MaxValue="20"/>
                        </Binding.ValidationRules>
                    </Binding>
                </TextBox.Text>
            </TextBox>
        </StackPanel>
        <Button Content="保存" Grid.Row="2" Click="Save_Click"/>
    </StackPanel>
</UserControl>
