<UserControl x:Class="MM.Medical.Client.Views.AddEndoscope"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MM.Medical.Client.Views"
             xmlns:controls="clr-namespace:Ms.Controls;assembly=Ms.Controls"
             Width="400" Height="300">
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Margin="10">
            <StackPanel Style="{StaticResource editorFormItem}">
                <TextBlock Text="生产厂家" Style="{StaticResource editorFormItemText}"/>
                <TextBox Text="{Binding Factory}" Style="{StaticResource editorFormItemInput}"/>

            </StackPanel>
            <StackPanel Style="{StaticResource editorFormItem}">
                <TextBlock Text="设备型号" Style="{StaticResource editorFormItemText}"/>
                <TextBox Text="{Binding Model}" Style="{StaticResource editorFormItemInput}"/>
            </StackPanel>

            <Button Click="Save_Click" Content="保存" IsDefault="True"/>
        </StackPanel>
    </Grid>
</UserControl>
